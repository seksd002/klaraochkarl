@page "/registrerad/{email}"
@inject KlarakarlServices service
<h1>Anmälan mottagen för @Person.Name </h1>
<p>Här är dina uppgifter</p>
<div class="container">
    <div class="row bg-light">
        <table class="table table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>Förnamn</th>
                    <th>Efternamn</th>
                    <th>Email</th>
                </tr>
            </thead>
            <tbody>
                @if (Person != null)
                {
                   
                        <tr>
                            <td>@Person.Name</td>
                            <td>@Person.Email</td>
                      
                        </tr>
                    
                }
                else
                {
                    <tr><td colspan="6"><strong>Hittade ingen person</strong></td></tr>
                }
            </tbody>
        </table>
    </div>
    </div>
<p>Du kommer få ett mail med kompletterande uppgifter inom kort håll utkik i din mailinkorg. Använd formuläret ovan för att korrigera.</p>
@code {
    [Parameter]
    public string? email { get; set; }

    Person Person = new Person();
    protected override async Task OnInitializedAsync()
    {
        await GetPerson(email);

    }
    private async Task GetPerson(string email)
    {
        //Person = await service.GetPerson(email);
    }
    public void sendmail()
    {
        //EmailClient emailClient = new EmailClient(connectionString);
    }
}
